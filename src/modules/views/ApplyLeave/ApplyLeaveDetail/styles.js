const { StyleSheet } = require("react-native");

export const styles = StyleSheet.create({
  mainContainer: {
    height: "100%",
    width: "100%",
    backgroundColor: "#e8fffa",
    padding: 15,
  },
  innerContainer: {
    width: "100%",
    padding: 10,
    minHeight: 90,
  },
});
